# Use for local Docker runs when Postgres runs on your host.
# Copy: cp .env.docker.example .env.docker
# Then: docker run -p 4000:4000 --env-file .env.docker social-platform-backend
#
# host.docker.internal = your host machine (Mac/Windows Docker Desktop).
# On Linux: add --add-host=host.docker.internal:host-gateway to docker run.

DATABASE_URL="postgresql://postgres:postgres@host.docker.internal:5432/social_platform_dev?schema=public"
PORT=4000
NODE_ENV=development
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
